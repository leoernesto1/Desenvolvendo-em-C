#include<conio.h>
#include<stdio.h>
main()
{
      int a, b;
      prinf("Digite o intervalo");
      scanf("%d", &a);
      printf("Digite o segundo numero");
      scanf("%d", &b);
      
      if(a>b)
      { 
             return 0;
      }
      else if{
           (a,b-1)+1 return 
      
      count_prime(a,b)=
      0, se a>b
      count_prime(a,b-1)+1, se prime(b)
      count_prime(a, b-1), senao
      
